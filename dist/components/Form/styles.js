import { makeStyles } from "@mui/styles";
export var useFormStyles = makeStyles(function (theme) { return ({
    root: {
        display: "flex",
        flexDirection: "column",
        gap: theme.spacing(1.5),
    },
}); });
