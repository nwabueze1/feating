import { BlockConfigType } from "../../types";
export declare const EuBlock8: ({ layouts, content, mode, index, screenSize, activeElement, handleSelect, configuration }: BlockConfigType) => import("react/jsx-runtime").JSX.Element;
