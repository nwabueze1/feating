import { makeStyles } from "@mui/styles";
export var useCartItemsStyles = makeStyles(function (theme) { return ({
    root: {
        display: "flex",
        flexDirection: "column",
        gap: theme.spacing(2),
    },
}); });
